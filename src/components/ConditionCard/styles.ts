import { StyleSheet } from "react-native"


export const styles = StyleSheet.create({
  container: {
    flex: 1,
    alignItems: "center",
    justifyContent: "center",
    height: 112,
    borderRadius: 16,
    padding: 16,
    gap: 8,
    overflow: "hidden",
  },
  divider: {
    width: "100%",
    height: 1,
  },
  infoContainer: {
    flex: 1,
    alignItems: "center",
    justifyContent: "center",
  },
})
